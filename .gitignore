# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# build/lint output logs — DO NOT commit
build-log.txt
build-output.txt
lint-output.txt
dev-output.txt
*.log

# Stripe transaction exports
stripe-transactions-*.xlsx

# Sentry Config File
.env.sentry-build-plugin
# Generated analysis files
translation-analysis.json

# Debug/error log files (generated during development)
errors.txt
check-errors.txt
ts-errors.txt
final-check.txt
build-errors.txt
build-errors.log

# Scripts directory — ignore one-off / local debug scripts.
# Tracked tooling is explicitly allowlisted below.
# (Use scripts/* not scripts/ so negation patterns can take effect.)
scripts/*
!scripts/check-locale-parity.mjs
# Reports t() keys that resolve to an object or are missing everywhere.
!scripts/check-i18n-keys.mjs
# Bundle guardrails invoked by `npm run build` (tfjs single-engine, lazy heavy
# libs) — must be committed or the build breaks for everyone else.
!scripts/check-bundle-guardrails.mjs
# Coverage tooling invoked by .github/workflows/ci.yml — must be committed.
!scripts/compare-coverage.mjs
!scripts/generate-coverage-badge.mjs
!scripts/ratchet-coverage.mjs
# Security tooling: lists public Convex functions with no authorization check.
!scripts/audit-convex-auth.mjs
# SOC 2 evidence collector (npm run soc2:evidence) — dated control report from
# repository state; part of the documented evidence plan (docs/soc2-type2-readiness.md §7.1).
!scripts/soc2-evidence.mjs
# CI audit gate (npm run audit:gate) — fails the Security Audit job on a new
# critical/high advisory. Missing from the repo => the job dies on a module-not-found.
!scripts/audit-gate.mjs
# Provisions the Sentry issue-alert rules (docs/observability.md §Sentry) — a DSN
# with no rule catches errors nobody reads; must be committed or the documented
# setup step points at a file that isn't there.
!scripts/sentry-alert-rules.mjs
# End-user guide generator (npm run docs:user-guide) — builds the .docx manual
# for the integration tabs, local payments and SOC 2 evidence tooling.
!scripts/generate-user-guide.mjs
!scripts/coverage-dashboard.mjs
# Generates convex/lib/localeCatalog.ts from public/locales — npm run
# build:convex-locales, and `-- --check` in CI to catch a stale catalog.
!scripts/build-convex-locales.mjs
# Bulk-translates untranslated locale keys via the AI providers (ru/de/hy).
!scripts/translate-untranslated.mjs
.vercel
.env*.local

# Lighthouse reports and analysis (temporary)
lighthouse-report-*.json
lighthouse-report-*.html
lighthouse-report*.json
lighthouse-report*.html
run-lighthouse.js
analyze-lighthouse.js

# JSON analysis files (temporary)
# numeric suffixes too (eslint-report2.json) — these get committed by accident
*-report.json
*-report[0-9]*.json
*-analysis.json

# Windows
Thumbs.db
desktop.ini

# Lighthouse scripts and reports
lighthouse-report*.json
lighthouse-report*.html
run-lighthouse-direct.mjs
run-lighthouse-puppeteer.mjs

# Temporary test/debug files
*.test-results.json
playwright-report/
test-results/

# IDE and editor files
.vscode/settings.json
.idea/
*.swp
*.swo
*~

# OS files
.DS_Store
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/

# Debug/output log files
lint-output*.txt
test-all-output*.txt
test-output*.txt
typecheck-output.txt
setup-hr-testing.sh

# ESLint report artifacts (generated during lint runs)
eslint_current.json
eslint_final_*.json
eslint_hooks*.json
eslint_lib*.json
eslint_nocache*.json
eslint_targets.json
eslint_report*.json
eslint_check*.json
eslint_fix*.json
eslint-ci-errors.json

# Reports and analysis directories
/reports/
tests/

# Audit/debug scripts (temporary)
audit-*.js
add-*.js
fix-*.js
check-*.js

# AI assistant local configs
.claude/
.agents/
skills-lock.json
AGENTS.md
CLAUDE.md
venv_reportnul
venv_report
nul

# OCR/tesseract local test artifacts (downloaded at runtime from CDN)
*.traineddata
test-passport.svg
